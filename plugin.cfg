[AUTHOR]
# Diese beiden Felder sind die KENNUNG des Plugins, kein Urhebervermerk.
# LoxBerry bildet daraus zusammen mit [PLUGIN] NAME die Kennzahl, unter der
# es Installation und Updates fuehrt. Sie duerfen spaeter NIE geaendert
# werden - LoxBerry hielte das Plugin sonst fuer ein anderes, und jedes
# Update schluege fehl. Deshalb steht hier ein Projektname und eine
# noreply-Adresse und keine Person.
NAME=Funkwacht Plugin Authors
EMAIL=funkwacht@users.noreply.github.com

[PLUGIN]
VERSION=1.0.1
NAME=funkwacht
FOLDER=funkwacht
TITLE=Funkwacht

[AUTOUPDATE]
# EINGESCHALTET seit 1.0.0. Bis 0.9.4 stand hier false mit der Begruendung,
# das Repository stehe noch nicht - das war ueberholt. Nachgemessen am
# 23.08.2026: github.com/timanders22/LoxBerry-Plugin-Funkwacht gibt es, es
# ist oeffentlich, der Vorgabezweig heisst master, und es liegen drei
# Releases (v0.9.1, v0.9.2, v0.9.3), keines davon ein Entwurf.
#
# DIE REIHENFOLGE BEIM VEROEFFENTLICHEN IST JETZT WICHTIG:
#   1. das Release mit dem Tag vX.Y.Z anlegen und nachsehen, dass es da ist
#   2. ERST DANACH die release.cfg im Zweig master auf diese Fassung setzen
# Andersherum bietet LoxBerry ein Archiv an, das es nicht gibt. Solange
# master eine kleinere Fassung nennt als die installierte, geschieht nichts -
# LoxBerry aktualisiert nur nach oben.
#
# Bei jedem Release: VERSION hier UND in release.cfg erhoehen, beide
# Adressen dort auf den neuen Tag setzen, das Release mit genau diesem Tag
# anlegen - und danach nachpruefen, dass der Tag wirklich existiert.
#
# Beide Adressen zeigen auf den Zweig master des EIGENEN Repositoriums - nie
# auf ein fremdes. LoxBerry vergleicht Fassungen als Zahl, und ein fremdes
# 2021.05.14 waere groesser als 1.0.0; daraus wuerde ein stilles Downgrade
# auf ein voellig anderes Plugin (Hausregel, belegt am Docker-Plugin).
AUTOMATIC_UPDATES=true
RELEASECFG=https://raw.githubusercontent.com/timanders22/LoxBerry-Plugin-Funkwacht/master/release.cfg
PRERELEASECFG=https://raw.githubusercontent.com/timanders22/LoxBerry-Plugin-Funkwacht/master/prerelease.cfg

[SYSTEM]
INTERFACE=2.0
REBOOT=false
# Mindestens 3.0.0: das Plugin liest config/system/general.json. Mit 2.0
# liesse es sich installieren und liefe dann nicht.
LB_MINIMUM=3.0.0
# Keine Obergrenze. plugininstall.pl ruft bei Ueberschreitung &fail() - die
# Installation wird abgebrochen, nicht nur gewarnt.
LB_MAXIMUM=false
# SYSTEM.ARCHITECTURE wird von plugininstall.pl gelesen und danach nie
# benutzt. Eine Angabe verhindert also nichts, waere aber eine unwahre
# Aussage - das Plugin ist Python und PHP und laeuft ueberall.
ARCHITECTURE=false
